# 用户发送消息到收到 AI 回复的完整链路

## 一、输入流转

### 1. 用户输入阶段
- 用户在输入框中输入文字，输入框会自动调整高度以适应内容
- 输入框实时监听内容变化，当有内容时发送按钮变为可用状态
- 用户可以通过点击发送按钮或按 Enter 键（非 Shift+Enter）发送消息

### 2. 消息提交阶段
- 输入组件捕获用户输入的文本，去除首尾空格
- 调用主页面传入的 `handleSend` 回调函数，将文本传递出去
- 清空输入框内容并重置高度，发送按钮恢复禁用状态

## 二、状态变化

### 1. 发送前状态
- `isLoading` 标志为 `false`，输入区域正常工作

### 2. 发送中状态
- 立即将 `isLoading` 设为 `true`，防止重复发送
- 在消息列表末尾添加加载指示器（"AI 正在思考..."）
- 页面自动滚动到底部

### 3. 收到回复后状态
- 移除加载指示器
- 将 `isLoading` 恢复为 `false`
- 输入区域恢复正常工作

### 4. 错误状态
- 如果 AI 服务返回错误，移除加载指示器
- 显示错误提示信息（"获取响应失败，请重试。"）
- `isLoading` 仍会恢复为 `false`，允许用户重试

## 三、数据存储

### 1. 消息结构
每条消息包含以下字段：
- `id`：唯一标识符（使用 `crypto.randomUUID()` 生成）
- `role`：消息角色（'user' 或 'ai'）
- `content`：消息内容
- `timestamp`：时间戳（毫秒）

### 2. 用户消息存储
- 调用 `HistoryManager.addMessage('user', text)` 添加用户消息
- 消息对象被添加到内存中的消息数组
- 整个消息数组被序列化后存入 `localStorage`，键名为 `'ai_chat_history'`

### 3. AI 回复存储
- 收到 AI 回复后，调用 `HistoryManager.addMessage('ai', responseText)` 存储 AI 消息
- 同样的存储流程，追加到消息数组并持久化到 `localStorage`

### 4. 历史消息加载
- 页面初始化时，从 `localStorage` 读取所有历史消息
- 如果没有历史记录，显示欢迎消息
- 如果有历史记录，按顺序渲染所有消息

## 四、渲染流程

### 1. 用户消息渲染
- 创建用户类型的聊天气泡（蓝色背景，右对齐）
- 气泡包含消息内容和时间戳
- 将气泡添加到消息列表容器
- 页面自动滚动到底部

### 2. 加载状态渲染
- 创建加载指示器组件（包含旋转动画和文字）
- 添加到消息列表末尾
- 页面自动滚动到底部

### 3. AI 回复渲染
- 先移除加载指示器
- 创建 AI 类型的聊天气泡（白色背景，左对齐）
- 气泡包含消息内容和时间戳
- 将气泡添加到消息列表容器
- 页面自动滚动到底部

### 4. 历史消息渲染
- 页面加载时遍历所有历史消息
- 逐条渲染但不触发滚动
- 所有消息渲染完成后，一次性滚动到底部（无平滑动画）

## 五、完整流程总结

1. **用户输入** → 输入框捕获文本，触发发送
2. **用户消息存储** → 写入 `localStorage`
3. **用户消息渲染** → 显示蓝色气泡，滚动到底部
4. **显示加载状态** → 显示"AI 正在思考..."
5. **调用 AI 服务** → 模拟网络延迟（1.5秒），根据关键词返回回复
6. **移除加载状态** → 隐藏加载指示器
7. **AI 回复存储** → 写入 `localStorage`
8. **AI 回复渲染** → 显示白色气泡，滚动到底部
9. **状态重置** → `isLoading` 恢复为 `false`，等待下一次输入
